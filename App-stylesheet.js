import { StyleSheet, Text, View } from "react-native";

export default function styles() {
  StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "#fff",
      alignItems: "center",
      justifyContent: "center",
    },
    bodyText: {
      fontFamily,
    },
  });
}
